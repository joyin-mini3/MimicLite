# @package _global_

defaults:
  - override /task: Go2M_gait
  - override /algo: ppo_hier

algo:
  mode: high

task:
  name: LocoManipHigh
  scene: locomanip
  observation:
    planner:
      door_state:   {}
  reward:
    high:
      survival: {weight: 0.01, enabled: true}
      door_handle:  {weight: 1.0, enabled: true}
      door_rotate:  {weight: 1.0, enabled: true}
      door_unlock:  {weight: 1.0, enabled: true}
      pass_through: {weight: 1.0, enabled: true}
  termination:
    impact_exceeds: {body_names: [Head.*, "base", ".*_thigh"], thres: 200}